import React, { FC } from 'react'
import { cn } from '@/lib/utils'

interface pageProps {
  
}

const Page: FC<pageProps> = ({   }) => {
  return (
    <div className={cn('')}>
     page
    </div>
  )
}
export default Page